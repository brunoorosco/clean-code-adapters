import {
  Controller,
  HttpRequest,
  HttpResponse
} from 'main/adapters/adapterExpress'

export class LoginController implements Controller {
  constructor() {}
  async handle(httpRequest: HttpRequest): Promise<HttpResponse> {
    try {
    } catch (error) {}
  }
}
